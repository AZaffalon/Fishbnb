<div class="row">
<h1 class='float-left'> Liste des poissons </h1>
<% if policy(Fish).create? %>
  <%= link_to 'Ajouter un poisson à la location', new_fish_path, class: 'textaligne_right btn btn-ghost btn-sm' %>
<% end %>
</div>
<div class="cards">
  <% @fishes.each do |fish|%>
    <div class="flex-item">
      <div class="card-category" style= "background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/breakfast.jpg)">
        <%= fish.name %>
      </div>
        <%= link_to 'See more', fish_path(fish), class: 'btn btn-gradient btn-sm'%>
        <%= link_to 'Change picture', '#', class: 'btn btn-gradient btn-sm' %>

    </div>
  <% end %>
</div>
