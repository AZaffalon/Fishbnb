<h1> Pour en savoir plus sur <%= @fish.name %></h1>
<p>To be completed</p>
<h2>Liste des réservations de <%= @fish.name %></h2>
<p>Propriétaire : <%= @fish.user.full_name %></p>
<table class= "table table-striped table-sm">
  <thead>
    <th>Du</th>
    <th>Au</th>
    <th>Disponibilité</th>
  <thead>
  <tbody>
    <% @fish.bookings.each do |booking| %>
    <tr>
      <td><%= booking.start_at %></td>
      <td><%= booking.end_at %></td>
    </tr>
    <% end %>

  </tbody>
</table>

<% if policy(@fish).edit? %>
  <%= link_to "Edit #{@fish.name}", edit_fish_path(@fish) %>
<% end %>
<% if policy(@fish).destroy? %>
  <%= link_to "Delete #{@fish.name}", fish_path(@fish),
  method: :delete, data: {confirm: "Are you sure?"} %>
<% end %>
